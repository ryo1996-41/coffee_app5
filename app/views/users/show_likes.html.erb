<div class="show-wrapper">
  <div class="container">
    <div class="user-show-width">
      <div class="show-info">
        <div class="show-image">
          <%= image_tag (@user.avatar)if @user.avatar.attached? %>
        </div>
        <div class="user-show-detail">
          <h2>  <%=@user.name%></h2>
          <strong class="post-counts">
            投稿<%=@user.posts.count%>件
          </strong>
          <span class="stats">
            <%= render 'users/stats' %>
          </span>
          <%= render 'users/follow_form' %>
          <div class="personal">
            <%if @current_user.id == @user.id  %>
            <p><%=@user.email%></p>
            <%end%>
            <%=@user.introduce if @user.introduce%><br>
          </div>
          <%if @current_user.id == @user.id  %>
          <div class="edit">
            <p>
              <%=link_to("編集", "/users/#{@user.id}/edit", class:"underline edit-btn") %>
            </p>
          </div>
          <%end%>
        </div>
      </div>

      <div class="post-like-head">
        <strong class="post-head">
          <%=link_to("投稿", "/users/#{@user.id}", class:"underline head-font") %>
        </strong>
        <strong class="like-head head-font">
          いいね
        </strong>
      </div>

      <div class="like-all">
        <div class="likes-detail">
          <% @posts_likes.each do |post_like| %>
          <%=link_to "/posts/#{post_like.id}" do%>
          <%=image_tag (post_like.image)%>
          <%end%>
          <%end%>
        </div>
      </div>
    </div>
    <div class="footer">
      <p>@coffee_app</p>
    </div>
  </div>
</div>
